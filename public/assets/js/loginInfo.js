//Function returns the (email) and (password) for:  router.post("/api/proximity", function(req, res)
//when we pass (email) & (password) as parameters for insertOne function.
var loginInfo = [];


//export for proximity_controller.js
module.exports = loginInfo;